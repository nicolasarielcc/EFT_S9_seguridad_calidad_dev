# Test-only overrides — these values are only used during unit/integration tests
app.security.user-password=test-user-pass
app.security.admin-password=test-admin-pass
backend.base-url=http://localhost:8081
